// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as Project14CEventBridgeVirtualLollyApp from '../lib/project14_c-event_bridge-virtual_lolly_app-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/project14_c-event_bridge-virtual_lolly_app-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new Project14CEventBridgeVirtualLollyApp.Project14CEventBridgeVirtualLollyAppStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
